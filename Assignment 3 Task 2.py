import math

# User input
num = float(input("Enter a number: "))

# Calculations
sqrt_val = math.sqrt(num)
log_val = math.log(num)
sin_val = math.sin(num)

# Display results
print(f"Square root: {sqrt_val}")
print(f"Natural log: {log_val}")
print(f"Sine (radians): {sin_val}")